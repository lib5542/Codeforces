#include <bits/stdc++.h>

using namespace std;

void solve() {
    vector<vector<int>> A = {{1, 2, 4, 8}};

    int step = 0;
    while (true) {
        vector<int> B = A[step];
        vector<int> C;

        for (auto& l : A) {
            // 1132 어케 만듦
        }

        A.push_back(B);
    }
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    /*
    소수 출력 사용 시
    cout << fixed;
    cout.precision(10);
    */

    solve();

    return 0;
}